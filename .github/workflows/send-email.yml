name: CI/CD Send GitHub Notification

on:
  workflow_run:
    workflows: ["CI/CD On Push Workflow"]
    types:
      - completed

  workflow_dispatch: 

jobs:
  read-pull-request:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Get last pull request number
        id: get-pr-number
        run: |
          pr_number=$(git log -1 --pretty=format:%s | grep -oP '#\d+')
          pr_number=${pr_number//#/}  # Remove '#' symbol
          echo "::set-output name=pr_number::$pr_number"    
          
      - name: Print pull request number
        run: echo "Last pull request number is ${{ steps.get-pr-number.outputs.pr_number }}"

      - name: Add Comment
        uses: actions/github-script@v5
        with:
          github-token: ${{secrets.GITHUB_TOKEN}}
          script: |
            github.rest.issues.createComment({
              issue_number: ${{ steps.get-pr-number.outputs.pr_number }},
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: 'Please change PR Title!!'
            })

   #    - name: list all closed pull requests in last 24 hours
   #      id: list
   #      uses: buildsville/list-pull-requests@v1
   #      with:
   #          token: ${{secrets.GITHUB_TOKEN}}
   #          state: 'closed'
   #          window: '24'
   #    - name: output
   #      run: echo '${{ steps.list.outputs.pulls }}'




# jobs:
#   send-notification:
#     runs-on: ubuntu-latest
#     if: ${{ github.event.workflow_run.conclusion == 'success' }}

#     steps:
#       - name: Get test results
#         id: get-results
#         uses: actions/github-script@v5
#         with:
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#           script: |
#             const { data: runs } = await github.rest.actions.listJobsForWorkflowRun({
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               run_id: ${{ github.event.workflow_run.id }}
#             });
#             const tests = runs.jobs.flatMap(job => job.steps.filter(step => step.name === 'Run unit tests'));
#             return tests.map(test => test.conclusion).join(", ");

#       - name: Send Github notification
#         uses: actions/github-script@v5
#         with:
#           github-token: ${{secrets.GITHUB_TOKEN}}
#           script: |
#             github.rest.issues.createComment({
#               issue_number: context.issue.number,
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               body: 'Please change PR Title!!'
#             })



